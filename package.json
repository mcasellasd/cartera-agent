{
  "name": "cartera-agent",
  "version": "1.0.0",
  "description": "Dashboard de cartera amb agent conversacional (OpenAI)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "macro:refresh": "node scripts/update-macro-cache.js",
    "test": "node scenario-model.test.js && node macro-store.test.js && node sheet-store.test.js && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "xlsx": "^0.18.5"
  },
  "private": true
}
